name: Sazlik Bot Calistir

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: # Manuel tetikleme butonu

jobs:
  build:
    runs-on: ubuntu-latest
    
    # İzinleri burada da açıkça belirtelim ki garanti olsun
    permissions:
      contents: write

    steps:
    - name: Depoyu Cek (Checkout)
      uses: actions/checkout@v4
      with:
        fetch-depth: 0 # Tüm geçmişi çek ki hata olmasın

    - name: Python Kurulumu (Setup Python)
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'
        cache: 'pip'

    - name: Kütüphaneleri Yukle (Install Dependencies)
      run: |
        python -m pip install --upgrade pip
        pip install feedparser nltk yfinance pandas

    - name: Botu Calistir (Run Bot)
      run: |
        python news_bot.py

    - name: Verileri Kaydet (Commit & Push)
      run: |
        git config --global user.name "Sazlik Bot"
        git config --global user.email "bot@sazlik.com"
        
        git add sazlik_signals.csv
        
        # Değişiklik yoksa hata verme, varsa işle
        git commit -m "🤖 Bot Raporu: Yeni Sinyaller Eklendi" || echo "Değişiklik yok"
        
        # --- KİLİT NOKTA BURASI ---
        git pull --rebase origin main
        git push origin main
